import 'package:flutter/cupertino.dart';

class AppPadding {
  AppPadding._();
  static const h24 = EdgeInsets.symmetric(horizontal: 24);
  static const h15 = EdgeInsets.symmetric(horizontal: 15);
  static const h50 = EdgeInsets.symmetric(horizontal: 50);
  static const t20 = EdgeInsets.only(top: 20);
  static const t50 = EdgeInsets.only(top: 50);
  static const t80 = EdgeInsets.only(top: 80);
}
